// Deprecated old landing community component. Use new landing layout files in `src/app/landing/components`.

'use client';

// Deprecated old landing community component. Use new landing layout files in `src/app/landing/components`.
export default function DeprecatedLandingCommunity() {
  return null;
}

// Deprecated old landing stats component: replaced by the new layout under `src/app/landing`.

'use client';

// Deprecated old landing stats component: replaced by the new layout under `src/app/landing`.
export default function DeprecatedLandingStats() {
  return null;
}
